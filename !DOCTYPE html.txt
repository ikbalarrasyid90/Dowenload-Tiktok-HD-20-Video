<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Batch Video Downloader UI</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body {
    font-family: Arial, sans-serif;
    background: #0f172a;
    color: #e5e7eb;
    margin: 0;
    padding: 20px;
}
.container {
    max-width: 800px;
    margin: auto;
    background: #020617;
    padding: 20px;
    border-radius: 10px;
}
h1 {
    text-align: center;
    margin-bottom: 10px;
}
textarea {
    width: 100%;
    height: 200px;
    background: #020617;
    color: #e5e7eb;
    border: 1px solid #334155;
    border-radius: 8px;
    padding: 10px;
    resize: none;
}
button {
    width: 100%;
    margin-top: 15px;
    padding: 12px;
    font-size: 16px;
    background: #22c55e;
    border: none;
    border-radius: 8px;
    cursor: pointer;
}
button:hover {
    background: #16a34a;
}
.progress {
    margin-top: 15px;
    height: 20px;
    background: #334155;
    border-radius: 10px;
    overflow: hidden;
}
.bar {
    height: 100%;
    width: 0%;
    background: #22c55e;
    transition: width 0.3s;
}
.results {
    margin-top: 20px;
}
.result-item {
    background: #020617;
    border: 1px solid #334155;
    padding: 10px;
    border-radius: 6px;
    margin-bottom: 8px;
    font-size: 14px;
}
.note {
    font-size: 12px;
    color: #94a3b8;
    margin-top: 10px;
    text-align: center;
}
</style>
</head>

<body>
<div class="container">
    <h1>Batch Video Processor</h1>
    <p>Paste up to 20 links (one per line)</p>

    <textarea id="links" placeholder="https://example.com/video1
https://example.com/video2"></textarea>

    <button onclick="processLinks()">Process Links</button>

    <div class="progress">
        <div class="bar" id="bar"></div>
    </div>

    <div class="results" id="results"></div>

    <p class="note">
        ⚠️ Demo UI only. No actual downloading performed.
    </p>
</div>

<script>
function processLinks() {
    const input = document.getElementById("links").value.trim();
    const links = input.split("\n").filter(l => l !== "").slice(0, 20);
    const results = document.getElementById("results");
    const bar = document.getElementById("bar");

    results.innerHTML = "";
    bar.style.width = "0%";

    if (links.length === 0) {
        alert("Please enter at least one link.");
        return;
    }

    let progress = 0;
    const step = 100 / links.length;

    links.forEach((link, index) => {
        setTimeout(() => {
            progress += step;
            bar.style.width = progress + "%";

            const div = document.createElement("div");
            div.className = "result-item";
            div.innerHTML = `✔ Processed: ${link}`;
            results.appendChild(div);
        }, index * 300);
    });
}
</script>
</body>
</html>
